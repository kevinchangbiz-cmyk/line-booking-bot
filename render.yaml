# Render 一鍵部署：https://render.com → New → Blueprint
services:
  - type: web
    name: line-booking-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run render:start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
